const { heroui } = require("@heroui/theme");

module.exports = heroui({
  themes: {
    light: {
      colors: {
        white: "#FFFFFF",
        dark: "#202020",
        default: "#e3e3e3",
      },
    },
  },
});
